<header class="grid">
  <section>
    <h1>Regeneracja siłowników hydraulicznych do&nbspkabrioletów</h1>
    <a href="/"
      >Zobacz zrealizowane projekty
      <i class="fas fa-chevron-right"></i>
    </a>
  </section>
  <img class="header-img" src="img/header.svg" alt="" />
</header>

<style lang="scss">
  header {
    width: 100%;
    height: 75vh;
    align-items: center;
    justify-content: baseline;
    background-size: cover;
    background-position-x: center;
    background-position-y: center;
    display: grid;
    grid-template-columns: repeat(2, calc(50% - 24px));
    grid-gap: 48px;

    section {
      flex: 1;
      max-width: 50%;

      h1 {
        margin: 16px 0;
      }

      a {
        display: inline-block;
        transition: ease-in;

        &::after {
          content: none;
        }

        i {
          transition: all 0.2s ease-in-out;
        }

        &:hover i {
          margin-left: 8px;
        }
      }
    }

    img {
      width: 100%;
      opacity: 0.7;
      object-fit: cover;
    }
  }
</style>
